name: flutter_back_end
description: A new Flutter project.

publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  vaid:
    git:
      url: https://trongtin045@bitbucket.org/vaway/vaid.git
  async: ^2.4.0
  cached_network_image: ^2.5.0
  crypto: ^2.1.5
  cupertino_icons: ^1.0.0
  device_info: ^1.0.0
  dio: ^3.0.10
  fl_chart: ^0.12.3
  flutter_custom_dialog: ^1.0.20
  flutter_slidable: ^0.5.7
  fluttertoast: ^7.1.8
  get: ^3.25.4
  image_picker: ^0.6.7+22
  intent: ^1.4.0
  permission_handler: ^5.1.0+2
  shared_preferences: ^0.5.12+4
  url_launcher: ^5.7.10
  uuid: ^2.2.2
dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:
  uses-material-design: true
  
  assets:
    - assets/images/
